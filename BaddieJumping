local Rayfield = loadstring(game:HttpGet('https://sirius.menu/rayfield'))()

local ReplicatedStorage = game:GetService("ReplicatedStorage")

-- ==================== ANTI-BAN HOOK ====================
local oldNamecall
oldNamecall = hookmetamethod(game, "__namecall", newcclosure(function(self, ...)
    if getnamecallmethod() == "FireServer" then
        local name = tostring(self):lower()
        if name:find("ban") or name:find("kick") or name:find("anticheat") or name:find("detect") then
            return
        end
    end
    return oldNamecall(self, ...)
end))

-- ==================== WINDOW ====================
local Window = Rayfield:CreateWindow({
   Name = "SandMan's Baddie Jumping Hub 💰💎🐾🌟",
   LoadingTitle = "Loaded",
   LoadingSubtitle = "by Mr.Sandman",
   ConfigurationSaving = {
      Enabled = true,
      FolderName = "SandManHub",
      FileName = "BaddieJumping_v17"
   },
   KeySystem = false
})

-- ==================== AUTO FARM TAB (FIRST TAB) ====================
local FarmTab = Window:CreateTab("🌟 Auto Farm", 4483362458)

FarmTab:CreateSection("Auto Farm")

local autoWins = false
local autoBubble = false
local autoRebirth = false

FarmTab:CreateToggle({
   Name = "🔄 Auto Wins",
   CurrentValue = false,
   Callback = function(Value)
      autoWins = Value
      if Value then
         task.spawn(function()
            while autoWins do
               ReplicatedStorage.Events.LayerChanged:FireServer("Layer6")
               task.wait(0.1)
            end
         end)
      end
   end
})

FarmTab:CreateToggle({
   Name = "🔄 Auto Bubble",
   CurrentValue = false,
   Callback = function(Value)
      autoBubble = Value
      if Value then
         task.spawn(function()
            while autoBubble do
               ReplicatedStorage.Remotes.Bubble:FireServer()
               task.wait(0.1)
            end
         end)
      end
   end
})

FarmTab:CreateToggle({
   Name = "🔄 Auto Rebirth",
   CurrentValue = false,
   Callback = function(Value)
      autoRebirth = Value
      if Value then
         task.spawn(function()
            while autoRebirth do
               ReplicatedStorage.Events.Rebirth:FireServer()
               task.wait(0.1)
            end
         end)
      end
   end
})

FarmTab:CreateButton({
   Name = "♻️ Rebirth Once",
   Callback = function()
      ReplicatedStorage.Events.Rebirth:FireServer()
      Rayfield:Notify({Title = "Rebirth Fired", Duration = 4})
   end
})

-- ==================== INF MONEY TAB ====================
local MoneyTab = Window:CreateTab("💰 Inf Money", 4483362458)
MoneyTab:CreateSection("Roulette Money Reward")

local moneyAmount = 1000000
local autoMoney = false

MoneyTab:CreateInput({
   Name = "Money Amount",
   PlaceholderText = "Type any amount here",
   RemoveTextAfterFocusLost = false,
   Callback = function(Value)
      local num = tonumber(Value)
      if num then moneyAmount = num end
   end
})

MoneyTab:CreateToggle({
   Name = "🔄 Auto Inf Money (0.1s)",
   CurrentValue = false,
   Callback = function(Value)
      autoMoney = Value
      if Value then
         task.spawn(function()
            while autoMoney do
               local args = {{value = moneyAmount, type = "Money", chance = 0.3}}
               ReplicatedStorage.Remotes.Roulette.GiveOutReward:FireServer(unpack(args))
               task.wait(0.1)
            end
         end)
      end
   end
})

-- ==================== INF GEMS TAB ====================
local GemsTab = Window:CreateTab("💎 Inf Gems", 4483362458)
GemsTab:CreateSection("ReachedGemReward Remote")

local gemAmount = 5000
local autoGems = false

GemsTab:CreateInput({
   Name = "Gems Amount",
   PlaceholderText = "Type any amount here",
   RemoveTextAfterFocusLost = false,
   Callback = function(Value)
      local num = tonumber(Value)
      if num then gemAmount = num end
   end
})

GemsTab:CreateToggle({
   Name = "🔄 Auto Inf Gems (0.1s)",
   CurrentValue = false,
   Callback = function(Value)
      autoGems = Value
      if Value then
         task.spawn(function()
            while autoGems do
               ReplicatedStorage.Remotes.ReachedGemReward:FireServer(gemAmount)
               task.wait(0.1)
            end
         end)
      end
   end
})

-- ==================== GET ANY PET TAB ====================
local PetTab = Window:CreateTab("🐾 Get Any Pet", 4483362458)

PetTab:CreateSection("Select Pet + Rarity")

local petList = {
   "Abyss", "Aqua Dragon", "Autumn Dragon", "Autumn Spirit", "Autumnal Master", "Bear",
   "Blocky Pegasus", "Blossom Protector", "Bumble Bee", "Cake Spider", "Candy Golem",
   "Cookie Corgi", "Crystal Kitty", "Cyber Cat", "Dark Champion", "Deer", "Diamond Serpent",
   "Dice Leviathan", "Doggy", "Dragon", "Emerald Bat", "Fancy Dragon", "Fire Deer",
   "Fire Fox", "Frosty Serpent", "Galactic Paladin", "Galaxy Doggy", "Gem Overlord",
   "Gold Ant", "Golden Butterfly", "Golden Hybrid", "Golem Plushie", "Halloween Cake",
   "Lava Fox", "Light Demon", "Lightning Demon", "Lucky Fiend", "Lucky Wyvern",
   "Magma Butterfly", "Matrix Robot", "Mega Fairy Chihuahua", "Mega King Ant", "Mega Lily",
   "Mega Sinister Pumpkin", "Mega Soul Golem", "Minty Serpent", "Mythic Autumnal Master",
   "Mythic Gem Shock", "Mythic King Slime", "Mythic Radiant One", "Mythic Vectorlord",
   "Neon Sky Bunny", "Neptunian Dragon", "Night Terror", "Phoenix Hybrid", "Plasma Wolf lord",
   "Plush Kitsune", "Radiant One", "Radiant Protector", "Rainbow Aqua Dragon", "Rubber Bunny",
   "Rubber Kitty", "Ruby Bat", "Santa Cat", "Shopkeeper", "Sky Imp", "Slime Overlord",
   "Space Bull", "Spore Serpent", "Starry Demon", "Summer Shark", "Tralalele Tralala",
   "Ultra Fire Fox", "Ultra Icecream Cake", "Ultra Waffle Cone", "Unicorn", "Venus Boar",
   "Waffle Cone", "Water Crab", "Witch", "Wolflord", "Yellow Dino"
}

local rarityList = {"Common", "Epic", "Exclusive", "Legendary", "Mythic", "Rare", "Secret", "Uncommon"}

local selectedPet = petList[1]
local selectedRarity = "Exclusive"
local autoPet = false

PetTab:CreateDropdown({
   Name = "Pet Name",
   Options = petList,
   CurrentOption = {selectedPet},
   Callback = function(Value)
      selectedPet = Value[1]
   end
})

PetTab:CreateDropdown({
   Name = "Pet Rarity",
   Options = rarityList,
   CurrentOption = {selectedRarity},
   Callback = function(Value)
      selectedRarity = Value[1]
   end
})

PetTab:CreateToggle({
   Name = "🔄 Auto Get Pet (0.1s)",
   CurrentValue = false,
   Callback = function(Value)
      autoPet = Value
      if Value then
         task.spawn(function()
            while autoPet do
               local args = {{name = selectedPet, type = "Pet", rarity = selectedRarity, chance = 0.3}}
               ReplicatedStorage.Remotes.Roulette.GiveOutReward:FireServer(unpack(args))
               task.wait(0.1)
            end
         end)
      end
   end
})

PetTab:CreateButton({
   Name = "🐾 Get Pet Once",
   Callback = function()
      local args = {{name = selectedPet, type = "Pet", rarity = selectedRarity, chance = 0.3}}
      ReplicatedStorage.Remotes.Roulette.GiveOutReward:FireServer(unpack(args))
      Rayfield:Notify({Title = "Pet Requested", Content = selectedPet .. " (" .. selectedRarity .. ")", Duration = 5})
   end
})

FarmTab:CreateButton({
   Name = "Destroy GUI",
   Callback = function() Rayfield:Destroy() end
})

Rayfield:Notify({
   Title = "Hub Updated v17",
   Content = "Auto Farm is now first tab • Rebirth moved inside",
   Duration = 8
})
